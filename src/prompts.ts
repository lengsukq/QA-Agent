export const prompts: Record<string, string> = {
  'qa-main.md': `Dispatch every QA request through the CLI-first modes in start.md, test.md, review.md, and archive.md. The host Skill mirrors the returned TodoList and performs the conversation; the Runtime owns state, evidence, reports, and archive gates.`,
  'start.md': `Start mode: read source and project memory as needed, then run qa-agent start --request "<request>" --module <id> --task <id>. Confirm taskDirectory, taskAssets, planHash, and TodoList. Present the generated Task background, requirements, scenarios, safety, cleanup, and Test Plan to the user. Stop at approval_required. Do not browse, use a browser, simulator, device, or UI tool during planning.`,
  'test.md': `Test mode: execute only an approved Task. Run qa-agent test --module <module> --task <task> [--scenario <scenario>] with the current platform, environment, role, and test-data context. The CLI selects explore when no compatible active OperationPlan exists and replay when one exists. Use UI tools only when uiExecutionAllowed=true, mustStop=false, and a runId are returned. Record screenshots, assertions, cleanup, and completion through the Runtime; never write a manual report or claim PASS.`,
  'review.md': `Review mode: approval happens in the host conversation. Explain the planHash and ask the human to explicitly confirm scope, business logic, scenarios, evidence, safety stops, and cleanup. After confirmation, the host may call the internal persistence command qa-agent task review <task> --module <module> --approve --confirmed-by <human>. The Agent cannot approve its own plan.`,
  'archive.md': `Archive mode: after a successful Runtime Run, run qa-agent archive --module <module> --task <task>. Archive is rejected unless the Task has complete background assets, current approval, every Scenario's active current-hash OperationPlan, a covering active RegressionSuite, a successful Runtime-owned report with existing screenshots and Markdown image evidence, and a passing project validation. Failed checks must not change Task state.`,
  'module-planner.md': `Create or reuse a business Module and a concrete Task before UI execution. Read source only to refine the plan. Cover the requested business outcome, important boundaries, permissions, failure states, and cleanup; do not execute during planning.`,
  'task-planner.md': `Keep the complete test asset under .qa-agent/modules/<module>/tasks/<task>/. Define concrete Scenarios, expected outcomes, assertions, evidence, test-data references, and cleanup. Present the plan and planHash, then wait for explicit approval from a real human reviewer; the Agent cannot approve its own plan.`,
  'execution.md': `Use task explore only for the first approved execution. Once an active OperationPlan exists, use operation replay and execute its JSON exactly in order without replanning or source review. Use only the runId returned with uiExecutionAllowed=true and mustStop=false. If capabilities are missing, truthfully attest available host tools with host attest or import a capability snapshot, run host doctor, and retry; never bypass Runtime preflight. Record every real UI action with run step and a screenshot. A passed run step never substitutes for run observe. Record each declared assertion with run observe, each declared cleanup with run cleanup, then call run complete. If any command returns mustStop=true, BLOCKED, or NEEDS_CONFIRMATION, stop UI execution immediately. Never create .qa-agent/reports/<name>.md or any separate PASS report. After a valid run complete, inspect status, operationCandidates, and operationCandidateIssues.`,
  'assertion.md': `Pass only against declared expectations and observable evidence. Use screenshots for rendered facts and API, data, or log evidence for non-visible facts. Never infer persistence from a successful click or success screen alone.`,
  'impact-analysis.md': `For release checks, map changed files to Modules and Tasks, preserve unmatched files, include required gates and Golden Paths, and explain selection reasons. A required P0 or release-gate Task without an active OperationPlan is NO-GO; other required asset gaps are REVIEW.`,
  'source-verification.md': `Use source code read-only after observing the result. Label source findings as supporting diagnosis, not proof of the business outcome.`,
  'memory-curator.md': `Store only durable, traceable project knowledge after review. Never store secrets or unredacted production data.`,
  'report.md': `Only QA-Agent Runtime may generate a formal Task report. Store each Run as one self-contained package under Task/runs/<run-id>/ with run.json, report.md, screenshots/, and evidence/. The report must contain the Runtime ownership marker, Run ID, evidence counts, and inline checkpoint screenshots. Never write Task reports under .qa-agent/reports/ or Task/reports/. The Runtime verdict is authoritative; include OperationPlan readiness issues and reference child reports from aggregate reports.`,
};
