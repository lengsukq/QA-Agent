export const prompts: Record<string, string> = {
  'start.md': `Start/discovery mode. Resolve or create the Draft Task with qa-agent start --request "<request>" --module <module> --task <task>. Before asking the user anything, inspect relevant project source, Module facts, reviewed memory, historical Runs, and existing OperationPlans with approved read-only tools. Record confirmed facts, assumptions, risks, scope, test data, cleanup, and requirement-to-Scenario coverage in the Task assets. Ask only for product, scope, risk, role, environment, data, or side-effect decisions that the project cannot answer, and ask at most one highest-value question per turn. Creating Task assets is non-destructive and does not need separate permission. Never use UI tools during discovery or planning.`,
  'review.md': `Review/approval mode. Present the Runtime-generated TestPlan, planHash, Scenario matrix, requirement coverage, in-scope and out-of-scope items, role/environment/test data, evidence policy, safety stops, cleanup, assumptions, and changes from any previously approved plan. TestPlan approval authorizes one or more executions of that unchanged business contract; it does not approve a reusable OperationPlan. After explicit approval from a real human, persist it with qa-agent review --module <module> --task <task> --approve --confirmed-by <human>. Do not start a Run or use UI tools in the same approval action.`,
  'test.md': `Test/execution mode. Execute only a currently approved Task with qa-agent test --module <module> --task <task> [--scenario <scenario>]. Runtime selects Explore for first-time discovery or Replay when an approved OperationPlan exists, together with the current environment, platform, role, device, build, and test-data context. An OperationPlan is a Scenario-specific, persisted and repeatable business operation path: it fixes the operation order, locators, input references, expected states, assertions, evidence, and cleanup. Runtime stores each generated OperationPlan under its owning Task at .qa-agent/modules/<module>/tasks/<task>/operation-plans/<scenario>/; never create or save an OperationPlan as a standalone project-level script. Every OperationPlan replay must begin from the application's home page or defined initial entry point, with the approved initial state restored or confirmed; never begin from an intermediate page or leftover state from a previous Run. Replay may be performed manually, with Agent assistance, or by a dedicated executor such as Playwright, Python plus fb-idb, or ADB; the executor must follow the persisted path and must not rediscover or re-plan the flow. Follow only the returned gates, allowedActions, nextActions, breadcrumb, and runId. Use UI tools only when uiExecutionAllowed=true and mustStop=false. Persist each actual action, screenshots, observed business assertion, cleanup result, evidence artifact, and recovery attempt through qa-agent run step/evidence/observe/cleanup/recover/complete. A passed step is not a passed business assertion. Never write a manual report or claim PASS before Runtime completion. Runtime automatically creates eligible OperationPlan candidates when a successful exploratory Run completes; do not call operation generate in the normal workflow.`,
  'result.md': `Result/promotion mode. Treat the Runtime report and persisted Run as authoritative. Explain the verdict, evidence, failed or blocked assertions, cleanup, recovery, and any OperationPlan candidates or candidate issues. An OperationPlan is a persisted, Scenario-specific replay path, not a requirement that one particular Agent perform the flow: it can be followed manually, with Agent assistance, or by a dedicated browser/device executor. Candidates belong to their owning Task and are stored under .qa-agent/modules/<module>/tasks/<task>/operation-plans/<scenario>/; do not copy or promote them into a standalone script directory. For each candidate, show its Scenario, source Run, planHash, ordered steps, inputs, locators, safety actions, checkpoints, and known limitations. TestPlan approval and OperationPlan approval are separate. After explicit human promotion approval, call qa-agent task operation review <task> --module <module> --operation <operation-id> --approve --confirmed-by <human>. Approval changes the lifecycle to approved_unverified; it does not prove replay validity.`,
  'regression.md': `Regression mode. Run qa-agent test again for an approved_unverified or validated OperationPlan. An OperationPlan is a fixed, Scenario-specific business operation path that can be executed manually, with Agent assistance, or by a dedicated executor such as Playwright, Python plus fb-idb, or ADB. Every replay must start from the application's home page or defined initial entry point and a restored or confirmed approved initial state, so runs do not depend on residual navigation or data from a previous Run. Replay the persisted OperationPlan JSON in strict order; do not rediscover the UI, review source to replace the path, skip or duplicate steps, or create a new plan. Use only limited semantic/accessibility locator adaptation that preserves business meaning, and record every adaptation. A completely executed replay contract changes the OperationPlan to validated and makes it eligible for RegressionSuite, release gates, and archive, even when the Run correctly reports a failed business assertion. Business FAIL belongs to the Run and must not invalidate the reusable operation path. Incomplete or incompatible replay does not validate the plan; use stale only for explicit contract drift or invalidation, then return to result review or planning.`,
  'recovery.md': `Recovery mode. Follow only Runtime-provided recovery nextActions. Preserve the current Task, Run, evidence, and resumeToken. Allowed recovery may include wait, refresh/back, restart app, reset approved sandbox data, reconnect MCP, fallback locator, or resume checkpoint. Never bypass missing permissions, capability gates, user approval, safety policy, environment mismatch, planHash drift, or stale OperationPlan checks. If product meaning or approved scope changed, return to planning and request a new TestPlan approval.`,
  'archive.md': `Archive mode. Run qa-agent archive --module <module> --task <task> only when the Runtime reports that all archive gates can be satisfied. Every required Scenario must have current approval, complete assets, a validated current-planHash OperationPlan, RegressionSuite coverage, successful Runtime-owned exploratory and replay/adapted reports, existing screenshot evidence, declared assertions, cleanup, and resolved or intentionally deferred memory candidates. OperationPlans are reusable fixed operation paths; their execution may be manual, Agent-assisted, or delegated to a browser/device executor, but every replay must begin from the application's home page or defined initial entry point with the approved initial state restored or confirmed. Replay must preserve the approved path and must not rely on leftover state. A failed archive inspection must preserve the Task and all evidence.`,
  'report.md': `Only QA-Agent Runtime may generate a formal Task report. Store each Run as a self-contained package under Task/runs/<run-id>/ with run.json, report.md, screenshots/, and evidence/. The report must contain the Runtime ownership marker, Run ID, verdict, evidence counts, Scenario assertions, cleanup, recovery, and inline checkpoint screenshots. Never create a competing manual PASS report. Explain OperationPlans as reusable fixed replay paths that may be executed by a person, an Agent with human assistance, or a dedicated executor; do not describe them as requiring a fully autonomous Agent. Surface Runtime-generated OperationPlan candidates and candidate issues after reporting; candidate generation is automatic and candidate approval remains a separate human gate.`,
};
