/**
 * Compatibility marker for projects that previously imported the local adapter.
 * UI automation belongs to the host Agent; the core runtime no longer ships an executor.
 */
export const playwrightAdapterRemoved = true;
