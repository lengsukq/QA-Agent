interface:
  display_name: "QA Agent"
  short_description: "Project-aware QA planning and safe execution"
  default_prompt: "Use $qa-agent, load the current project canonicalPrompts, require real human plan approval, then execute and report the safe QA lifecycle."
